using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text;
using AltV.Net;
using AltV.Net.Elements.Entities;
using Checkpoint = Lambda.Entity.Area;

namespace Lambda.FactoryStuff
{
    /*class CheckpointFactory : ICustomFactory<ICheckpoint>
    {
        public ICheckpoint Create(IntPtr checkpointPointerPtr, ushort id)
        {
            return new Checkpoint(checkpointPointerPtr, id);
        }
    }*/
}
